<thead>
  <tr>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Title
          </div>
        </div>
        <div class="pure-u">
          <div id="title" class="actions-container">
            <%
              if(sort_field === "title") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    </th>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Report Start Date
          </div>
        </div>
        <div class="pure-u">
          <div id="report_start_date" class="actions-container">
            <%
              if(sort_field === "report_start_date") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    </th>
    <th id="report_end_date">
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Report End Date
          </div>
        </div>
        <div class="pure-u">
          <div id="report_end_date" class="actions-container">
            <%
              if(sort_field === "report_end_date") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    </th>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Reoccurring
          </div>
        </div>
        <div class="pure-u">
          <div id="recalculate_dates" class="actions-container">
            <%
              if(sort_field === "recalculate_dates") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    </th>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Status
          </div>
        </div>
        <div class="pure-u">
          <div id="status" class="actions-container">
            <%
              if(sort_field === "status") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Updated At
          </div>
        </div>
        <div class="pure-u">
          <div id="updated_at" class="actions-container">
            <%
              if(sort_field === "updated_at") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    <th>
      <div class="pure-g header-container">
        <div class="pure-u title-container">
          <div class="title">
            Created At
          </div>
        </div>
        <div class="pure-u">
          <div id="created_at" class="actions-container">
            <%
              if(sort_field === "created_at") {
            %>
                <i class="<%= sort_direction === 'asc' ? 'icon-sort-by-attributes' : 'icon-sort-by-attributes-alt'%>"></i>
            <%
              } else {
            %>
                <i class="icon-sort"></i>
            <%
              }
            %>
          </div>
        </div>
      </div>
    </th>
    <th></th>
  </tr>
</thead>
<tbody></tbody>