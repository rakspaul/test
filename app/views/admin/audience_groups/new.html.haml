#details.content

:javascript
  $(document).ready(function() {
    ReachUI.SetupGlobalErrorHandler();

    var controller = new ReachUI.AudienceGroups.AudienceGroupController();
      controller.initializeForCreateNewAudienceGroup();
  });