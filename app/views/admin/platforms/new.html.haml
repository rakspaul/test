.platforms-header
  %legend
    Add new Platform

#details.content

:javascript
  $(document).ready(function() {
    ReachUI.SetupGlobalErrorHandler();

    var controller = new ReachUI.Platforms.PlatformController();
    controller.initializeForCreateNewPlatform();
  });