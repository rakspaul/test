.platforms-header
  %legend
    Edit Platform

#details.content

:javascript
  $(document).ready(function() {
    ReachUI.SetupGlobalErrorHandler();

   var controller = new ReachUI.Platforms.PlatformController();
    controller.initializeForEditPlatform('#{@platform.id}');
  });